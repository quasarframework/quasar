{
  "name": "<%= pkg.name %>",
  "version": "<%= pkg.version %>",
  "description": "<%= pkg.description %>",
<% if (pkg.author) { %>  "author": "<%= pkg.author %>",<% } %>
  "private": true,
  "dependencies": {
    "@capacitor/cli": "^1.0.0",
    "@capacitor/core": "^1.0.0",
    "@capacitor/android": "^1.0.0",
    "@capacitor/ios": "^1.0.0"
  }
}
