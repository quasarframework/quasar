name: UI Tests

on:
  pull_request:
    types:
      - opened
    branches:    
      - 'AutomatingTests'
    paths:
      - 'ui/**'

jobs:
  UI Tests:

    runs-on: ubuntu-latest

    steps:
      - name: Run sample code
        run: echo "Success"