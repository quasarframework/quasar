---
title: Accessibility
desc: How Quasar components implement WAI-ARIA semantics, keyboard navigation and focus management — and what remains your app's responsibility.
related:
  - /options/quasar-language-packs
  - /options/rtl-support
  - /vue-components/list-and-list-items
---

Accessibility (often abbreviated "a11y") means your website or app can be used by everyone — including people relying on screen readers, keyboard-only navigation, voice control or other assistive technology (AT). Quasar components ship with a substantial accessibility layer: semantic markup, [WAI-ARIA](https://www.w3.org/TR/wai-aria-1.2/) attributes, keyboard interaction patterns and focus management. This page describes what the framework does for you, what it deliberately leaves to your app, and — component by component — where to find the details.

Accessibility is a contract between the framework and your app. Quasar can render a correct `role="checkbox"` with `aria-checked` and Space/Enter handling, but it cannot know what an icon-only button _means_, whether your brand colors have enough contrast, or which landmark a drawer represents. The [component matrix](#components-at-a-glance) below tells you exactly which side of the contract each piece lives on.

## What Quasar provides

### Semantic markup

Components render native HTML elements wherever one exists for the job — a real `<button>` (QBtn), `<a href>` (router links everywhere), `<form>` (QForm), `<table>` (QTable), `<hr>` (QSeparator), and the full landmark set from the [QLayout](/layout/layout) family: `<header>`, `<footer>`, `<aside>` and `<main>`. Native elements carry their semantics, states and keyboard behavior for free, which is always more robust than re-creating them with ARIA.

Where the visual design requires custom elements, components declare the matching ARIA role and manage its required states: `role="checkbox"`/`"radio"`/`"switch"` with `aria-checked` (including `mixed`), `role="slider"`/`"spinbutton"` with `aria-valuemin/max/now`, `role="progressbar"`, `role="tablist"`/`"tab"`/`"tabpanel"`, `role="tree"`/`"treeitem"` (with `aria-level`/`aria-setsize`/`aria-posinset` compensating for virtual scrolling), `role="combobox"` + `"listbox"`/`"option"` (QSelect), and `role="separator"` with full keyboard resizing (QSplitter).

Some containers deliberately claim **no** role, because they can host arbitrary content and a wrong role would produce invalid markup. [QMenu](/vue-components/menu#accessibility) is the canonical example: ARIA's `menu` role permits nothing but menu items as children, so QMenu stays neutral and you declare `role="menu"` on the [QList](/vue-components/list-and-list-items#accessibility) inside it when the content really is a list of commands. QItems then derive their roles from context — `menuitem` inside a declared menu, `listitem` inside a list, `button` when clickable, a plain link when they navigate. See the QList/QItem Accessibility section for the full derivation contract.

Decorative elements are hidden from AT: every [QIcon](/vue-components/icon#accessibility) renders `aria-hidden="true"` by default (interactive icons like a field's clear button opt back in with a role and a localized label), and backdrops, virtual-scroll padding, custom scrollbars and similar plumbing are all `aria-hidden`.

### Keyboard support

- **Activation**: everything clickable is keyboard-activatable. QBtn even synthesizes Space activation on link-shaped (`<a>`) buttons, which natively respond only to Enter. Clickable QItems, chips, expansion headers, sortable table headers and stepper headers all handle <kbd>Enter</kbd>/<kbd>Space</kbd>.
- **Composite widgets use roving tabindex** — the whole widget is a single Tab stop and arrow keys move within it, per the [WAI-ARIA APG](https://www.w3.org/WAI/ARIA/apg/): QTabs, QOptionGroup (radio mode), QRating, QDate's calendar, QCarousel's navigation, QEditor's toolbar and QTree.
- **Value widgets** respond to arrow keys (QSlider, QRange, QKnob, QSplitter, QTime's spinbuttons), most with <kbd>Home</kbd>/<kbd>End</kbd> and <kbd>PageUp</kbd>/<kbd>PageDown</kbd> variants; QSelect implements the full combobox keyboard contract including typeahead.
- **<kbd>Escape</kbd> dismissal** works as a chain: only the top-most open popup (dialog, menu, tooltip, drawer in overlay mode) reacts, closing popups one at a time. Persistent popups shake or ignore instead of closing.
- **Horizontal arrow keys are RTL-aware** throughout ([RTL support](/options/rtl-support)) — tabs, radio groups, sliders, date navigation, editor toolbar and splitter all mirror direction under RTL language packs.

Note that Escape handling and focus recapture are desktop-mode behaviors; on mobile platforms, dismissal is wired to the platform back button through Quasar's History plugin (Capacitor/Cordova builds).

### Focus management

- **Dialogs** record the element that opened them, move focus into the dialog when it renders (honoring `autofocus`/`data-autofocus`), recapture focus that strays out while modal, and restore focus to the opener on close.
- **Menus** do the same, with one refinement: <kbd>Tab</kbd> from the menu's last focusable element (or <kbd>Shift</kbd>+<kbd>Tab</kbd> from the first) closes the menu and continues tabbing from the anchor — keyboard focus can never leak into the void behind a portal.
- Focus moves are queued while popup transitions play, so competing focus intents can't race; a menu opened inside a modal dialog renders _inside_ the dialog's element so `aria-modal` doesn't hide it from AT.
- **QForm** focuses the first invalid field after failed validation.
- **The focus ring is keyboard-only**: after mouse or touch interaction, components park focus on an invisible helper so no ring flashes; keyboard focus keeps a visible indicator. This is built in — no `:focus-visible` polyfill needed. (The built-in indicator styling targets desktop mode; hybrid touch-plus-keyboard apps may want their own focus styles.)

### Screen reader labels and language packs

Built-in control labels — a field's clear button, a chip's remove icon, expand/collapse arrows, pagination's first/prev/next/last, QDate's month/year navigation, carousel arrows, the editor toolbar — come from the active [Quasar Language Pack](/options/quasar-language-packs) and are announced in your app's language automatically. If you author a custom language pack, cover these keys (several, like `label.expand`, are functions); screen reader users get whatever the pack provides.

### ARIA attributes fall through

Any `aria-*` attribute (or `role`) you place on a Quasar component reaches the semantically correct element: on form fields it lands on the native `<input>`/focus target, on QDialog it lands on the `role="dialog"` element, and so on. Your attributes win over generated ones (with `aria-describedby` on fields being _merged_, not clobbered). This is the standard mechanism for everything in the "your responsibility" list below — no special props needed.

## What your app is responsible for

**Accessible names.** Nothing can generate a meaningful name for you. Provide `aria-label` (or visible text) for: icon-only QBtns, FABs and QUploader's buttons; `alt` for QImg; `title` for QVideo; `aria-label`/`aria-labelledby` for QDialog, QDrawer, QToolbar, QOptionGroup and any `role="navigation"`/`role="group"` container you want distinguishable. Multiple unlabeled landmarks or toolbars are a WCAG failure.

**Popup semantics on custom triggers.** QBtnDropdown and QSelect fully wire their trigger ARIA (`aria-expanded`, conditional `aria-controls`). A bare QMenu on an arbitrary anchor does not — add `aria-expanded` state and, if you give the popup content a role, mirror it with `aria-haspopup` on the trigger. See [QMenu](/vue-components/menu#accessibility) and [QBtnDropdown](/vue-components/button-dropdown#accessibility).

**Landmark structure.** The QLayout family gives you one of each landmark for free — do not add your own `<main>` (QPage already is one), and label multiple drawers or navigation areas so they can be told apart.

**Color contrast.** Quasar ships no contrast enforcement, in light or [dark mode](/style/dark-mode). Check your [brand palette](/style/color-palette) against WCAG 2.2 contrast minima (4.5:1 for normal text) with a tool like the [WebAIM contrast checker](https://webaim.org/resources/contrastchecker/) — in both themes if you support both.

**Reduced motion.** Only the [animation CSS helpers](/options/animations) respect `prefers-reduced-motion`. Component transitions, the ripple and scroll-driven effects (QParallax) do not — motion-sensitive users need you to tone these down yourself (e.g. `$q.config.ripple = false`, transition props, disabling autoplay). QCarousel's `autoplay` never pauses on hover or focus; provide a pause control if you use it (WCAG 2.2.2).

**Announcing async state.** Loading indicators — QSpinner, QInnerLoading, QSkeleton, QAjaxBar, QUploader's per-file status — are visual only. Wrap or accompany them with a live region (`role="status"` plus a short text like "Loading…") when the state change matters to the user, and hide purely decorative placeholders with `aria-hidden="true"`.

**Keyboard alternatives for gestures.** Swipe-to-act (QSlideItem), pull-to-refresh and touch panning have no keyboard path. Offer a parallel affordance — visible buttons, a context menu, or QPullToRefresh's `trigger()` method wired to a button.

**Testing.** No framework replaces testing. Walk your critical flows with keyboard only (is everything reachable? is focus visible? can you get back out?); run a screen reader pass (VoiceOver ships with macOS/iOS, [NVDA](https://www.nvaccess.org/) is free on Windows); and add automated checks with [axe](https://www.deque.com/axe/) or Lighthouse to CI — they catch the mechanical failures (names, contrast, ARIA validity) so your manual passes can focus on flow.

## Components at a glance

Each component name links to the Accessibility section of its documentation page, which describes the exact behavior — including its limits — and what you should add on top.

### Buttons

| Component                                                     | Built-in behavior                                                                                                                    | Keyboard                                                           |
| ------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------ |
| [QBtn](/vue-components/button#accessibility)                  | Native `<button>` or `<a>` (`role="button"` derived where needed); `aria-disabled`; progressbar ARIA while loading with `percentage` | <kbd>Enter</kbd>/<kbd>Space</kbd>, including Space on link buttons |
| [QBtnDropdown](/vue-components/button-dropdown#accessibility) | Disclosure pattern: `aria-expanded`, `aria-controls` (while the popup exists), localized expand/collapse label                       | Inherited from QBtn + QMenu                                        |
| [QBtnGroup](/vue-components/button-group#accessibility)       | Visual grouping only — add `role="group"` + `aria-label` yourself                                                                    | Each button is its own Tab stop                                    |
| [QBtnToggle](/vue-components/button-toggle#accessibility)     | `aria-pressed` per option; per-option `attrs` for labels                                                                             | <kbd>Enter</kbd>/<kbd>Space</kbd> per button                       |
| [QFab](/vue-components/floating-action-button#accessibility)  | `aria-expanded`/`aria-controls` on the trigger; closed actions hidden from AT; focus returns to trigger                              | <kbd>Enter</kbd>/<kbd>Space</kbd> opens and activates              |

### Navigation

| Component                                                                                     | Built-in behavior                                                                             | Keyboard                                                                                 |
| --------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------- |
| [QTabs](/vue-components/tabs#accessibility)                                                   | `role="tablist"`/`"tab"` with `aria-selected`, `aria-orientation`                             | Roving tabindex; arrows (RTL-aware), <kbd>Home</kbd>/<kbd>End</kbd>; explicit activation |
| [QTabPanels](/vue-components/tab-panels#accessibility)                                        | `role="tabpanel"`, `tabindex="0"`; tab↔panel id wiring is manual (documented)                 | Panel reachable via <kbd>Tab</kbd>                                                       |
| [QBreadcrumbs](/vue-components/breadcrumbs#accessibility)                                     | Native links; no landmark/`aria-current` — wrap in `<nav>` and mark the current page yourself | Native link behavior                                                                     |
| [QPagination](/vue-components/pagination#accessibility)                                       | `role="navigation"`; localized first/prev/next/last labels; `aria-current` on the active page | <kbd>Enter</kbd>/<kbd>Space</kbd> on buttons; <kbd>Enter</kbd> commits input mode        |
| [QStepper](/vue-components/stepper#accessibility)                                             | `aria-current="step"`; each step a labeled `group`; navigable headers exposed as buttons      | <kbd>Enter</kbd>/<kbd>Space</kbd> on navigable headers                                   |
| [QToolbar](/vue-components/toolbar#accessibility) / [QBar](/vue-components/bar#accessibility) | `role="toolbar"` — name it via `aria-label` when a page has several                           | Children are individual Tab stops                                                        |

### Form fields

| Component                                                                                     | Built-in behavior                                                                                                                                                                                       | Keyboard                                                                                                                                                |
| --------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [QField](/vue-components/field#accessibility) / [QInput](/vue-components/input#accessibility) | `<label for>` wiring; errors announced via `role="alert"` with `aria-invalid`/`aria-errormessage`/`aria-describedby` (only while the message renders); accessible clear button                          | Native editing; <kbd>Enter</kbd>/<kbd>Space</kbd> on the clear button                                                                                   |
| [QSelect](/vue-components/select#accessibility)                                               | Full combobox pattern: `role="combobox"` + `aria-expanded`/`aria-controls`/`aria-activedescendant`, `role="listbox"`/`"option"` with `aria-selected` and virtual-scroll-aware `aria-setsize`/`posinset` | Arrows open/navigate, typeahead, <kbd>Home</kbd>/<kbd>End</kbd>, <kbd>PageUp</kbd>/<kbd>PageDown</kbd>, <kbd>Enter</kbd> selects, <kbd>Esc</kbd> closes |
| [QForm](/vue-components/form#accessibility)                                                   | Native `<form>`; focuses the first invalid field on failed validation                                                                                                                                   | Native submit                                                                                                                                           |
| [QEditor](/vue-components/editor#accessibility)                                               | `role="textbox"` + `aria-multiline`; toolbar follows the APG toolbar pattern with localized labels                                                                                                      | Toolbar roving tabindex, arrows/<kbd>Home</kbd>/<kbd>End</kbd>; <kbd>Ctrl</kbd> formatting shortcuts                                                    |
| [QFile](/vue-components/file#accessibility)                                                   | Field frame + validation ARIA; keyboard-openable picker                                                                                                                                                 | <kbd>Enter</kbd>/<kbd>Space</kbd> opens the picker                                                                                                      |
| [QUploader](/vue-components/uploader#accessibility)                                           | Real buttons and progressbar ARIA — but you must label the icon-only buttons and announce status                                                                                                        | Native button activation                                                                                                                                |

### Form controls

| Component                                                                                       | Built-in behavior                                                                                                      | Keyboard                                                                                                                                 |
| ----------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| [QCheckbox](/vue-components/checkbox#accessibility)                                             | `role="checkbox"`, three-state `aria-checked` (incl. `mixed`), `aria-label` from `label`                               | <kbd>Enter</kbd>/<kbd>Space</kbd> toggles                                                                                                |
| [QRadio](/vue-components/radio#accessibility)                                                   | `role="radio"` + `aria-checked`; group semantics come from QOptionGroup                                                | <kbd>Enter</kbd>/<kbd>Space</kbd> selects                                                                                                |
| [QToggle](/vue-components/toggle#accessibility)                                                 | `role="switch"` + `aria-checked`                                                                                       | <kbd>Enter</kbd>/<kbd>Space</kbd> toggles                                                                                                |
| [QOptionGroup](/vue-components/option-group#accessibility)                                      | `role="radiogroup"`/`"group"`; APG radio-group pattern                                                                 | Roving tabindex; arrows select (RTL-aware), skipping disabled options                                                                    |
| [QSlider](/vue-components/slider#accessibility) / [QRange](/vue-components/range#accessibility) | `role="slider"` with `aria-valuemin/max/now`, `aria-orientation`, `aria-readonly` (QRange: one named slider per thumb) | Arrows step (RTL/vertical-aware), <kbd>PageUp</kbd>/<kbd>PageDown</kbd> ×10                                                              |
| [QRating](/vue-components/rating#accessibility)                                                 | Radio-group pattern per star with per-star labels (`icon-aria-label`)                                                  | Roving tabindex; arrows move, <kbd>Enter</kbd>/<kbd>Space</kbd> select                                                                   |
| [QKnob](/vue-components/knob#accessibility)                                                     | `role="slider"` with value ARIA on the focusable element                                                               | Arrows step, <kbd>PageUp</kbd>/<kbd>PageDown</kbd> ×10                                                                                   |
| [QColor](/vue-components/color-picker#accessibility)                                            | Tune tab (native inputs + sliders) is the accessible path; palette/spectrum are pointer-only                           | Native inputs + slider keys on the Tune tab                                                                                              |
| [QDate](/vue-components/date#accessibility)                                                     | Day buttons with full-date labels, `aria-pressed` selection, `aria-current="date"` for today; localized navigation     | Roving tabindex; arrows cross months, <kbd>Home</kbd>/<kbd>End</kbd>, <kbd>PageUp</kbd>/<kbd>PageDown</kbd> (+<kbd>Shift</kbd> for year) |
| [QTime](/vue-components/time#accessibility)                                                     | Header `role="spinbutton"` per unit with localized labels and value ARIA; clock face is a pointer-only visualization   | Arrows step, <kbd>Home</kbd>/<kbd>End</kbd>, direct digit entry                                                                          |

### Popups

| Component                                                | Built-in behavior                                                                                                                             | Keyboard                                                                                 |
| -------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------- |
| [QDialog](/vue-components/dialog#accessibility)          | `role="dialog"` + `aria-modal`; focus moved in on open, contained while modal, restored on close — name it via `aria-label`/`aria-labelledby` | <kbd>Esc</kbd> dismisses (persistent dialogs shake instead)                              |
| [QMenu](/vue-components/menu#accessibility)              | Role-less popup by design; declare `role="menu"` on the QList inside for menu semantics; focus restored to the anchor on close                | <kbd>Esc</kbd> closes; <kbd>Tab</kbd> past the edge closes and continues from the anchor |
| [QTooltip](/vue-components/tooltip#accessibility)        | `role="tooltip"`; wires itself into the anchor's `aria-describedby` while shown; shows on keyboard focus                                      | <kbd>Esc</kbd> dismisses without moving focus (WCAG 1.4.13)                              |
| [QPopupProxy](/vue-components/popup-proxy#accessibility) | Switches between QMenu and QDialog by screen size — semantics follow the rendered component                                                   | Delegated                                                                                |
| [QPopupEdit](/vue-components/popup-edit#accessibility)   | QMenu-based edit surface; localized Set/Cancel buttons; never commits silently on dismissal                                                   | <kbd>Esc</kbd> cancels; wire <kbd>Enter</kbd>-to-save yourself                           |

### Lists and data

| Component                                                          | Built-in behavior                                                                                                                               | Keyboard                                                                                                                           |
| ------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| [QList / QItem](/vue-components/list-and-list-items#accessibility) | Context-derived roles: `list`/`listitem`, `menu`→`menuitem`, clickable→`button`, links stay links; `aria-disabled` on disabled actionable items | <kbd>Enter</kbd>/<kbd>Space</kbd> on clickable items                                                                               |
| [QExpansionItem](/vue-components/expansion-item#accessibility)     | Header `role="button"` with `aria-expanded`/`aria-controls` and localized expand/collapse label; collapsed content truly hidden                 | <kbd>Enter</kbd>/<kbd>Space</kbd> toggles                                                                                          |
| [QTable](/vue-components/table#accessibility)                      | Native `<table>`; sortable headers with `aria-sort` and keyboard sorting; localized pagination controls                                         | <kbd>Enter</kbd>/<kbd>Space</kbd> sorts; standard controls elsewhere                                                               |
| [QMarkupTable](/vue-components/markup-table#accessibility)         | Native `<table>` wrapper — header/scope/caption semantics are yours to author                                                                   | Native                                                                                                                             |
| [QTree](/vue-components/tree#accessibility)                        | `role="tree"`/`"treeitem"`/`"group"` with `aria-expanded`/`selected`/`checked`; virtual mode adds `aria-level`/`setsize`/`posinset`             | Roving tabindex; arrows navigate/expand/collapse, <kbd>Home</kbd>/<kbd>End</kbd>, <kbd>Enter</kbd> selects, <kbd>Space</kbd> ticks |
| [QVirtualScroll](/vue-components/virtual-scroll#accessibility)     | Padding hidden from AT; focus kept off `<body>` on slice changes; off-screen items don't exist for AT                                           | Scrollable container is a Tab stop when it owns scrolling                                                                          |
| [QTimeline](/vue-components/timeline#accessibility)                | Native `<ul>`/`<li>` structure                                                                                                                  | Static content                                                                                                                     |
| [QChatMessage](/vue-components/chat#accessibility)                 | Text content readable; sent/received is visual-only — convey authorship via `name`                                                              | Static content                                                                                                                     |
| [QCarousel](/vue-components/carousel#accessibility)                | Navigation as a `tablist` with roving tabindex and per-slide labels; slides as `tabpanel`s; localized arrows                                    | Arrows (orientation- and RTL-aware), <kbd>Home</kbd>/<kbd>End</kbd>; selection follows focus                                       |

### Feedback and media

| Component                                                                                                                               | Built-in behavior                                                                                                | Keyboard                                            |
| --------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- | --------------------------------------------------- |
| [QBadge](/vue-components/badge#accessibility)                                                                                           | `role="status"` (polite live region), `aria-label` from `label`                                                  | —                                                   |
| [QBanner](/vue-components/banner#accessibility)                                                                                         | `role="alert"` — announces when inserted dynamically                                                             | —                                                   |
| [QChip](/vue-components/chip#accessibility)                                                                                             | Clickable chips are `role="button"` with `aria-pressed`; remove icon is keyboard-operable with a localized label | <kbd>Enter</kbd>/<kbd>Space</kbd> activates/removes |
| [QLinearProgress](/vue-components/linear-progress#accessibility) / [QCircularProgress](/vue-components/circular-progress#accessibility) | `role="progressbar"` with value ARIA (dropped while indeterminate) — add an `aria-label`                         | —                                                   |
| [QAjaxBar](/vue-components/ajax-bar#accessibility)                                                                                      | Progressbar ARIA while active, `aria-hidden` when idle                                                           | —                                                   |
| [QSpinner](/vue-components/spinners#accessibility)                                                                                      | None — pair with a live region or hide as decorative                                                             | —                                                   |
| [QInnerLoading](/vue-components/inner-loading#accessibility)                                                                            | Visual overlay only — announce the state and manage the covered content yourself                                 | —                                                   |
| [QSkeleton](/vue-components/skeleton#accessibility)                                                                                     | Decorative placeholder — mark loading regions yourself                                                           | —                                                   |
| [QIcon](/vue-components/icon#accessibility)                                                                                             | `aria-hidden="true"` always; override via attrs for semantic icons                                               | —                                                   |
| [QImg](/vue-components/img#accessibility)                                                                                               | `role="img"` named by `alt` — always provide it                                                                  | —                                                   |
| [QAvatar](/vue-components/avatar#accessibility) / [QCard](/vue-components/card#accessibility)                                           | Presentational containers; `tag` prop for semantic structure                                                     | —                                                   |
| [QSeparator](/vue-components/separator#accessibility)                                                                                   | Native `<hr>` with `aria-orientation`                                                                            | —                                                   |
| [QVideo](/vue-components/video#accessibility)                                                                                           | `<iframe>` named by the `title` prop — always provide it                                                         | Embedded player's own                               |
| [QParallax](/vue-components/parallax#accessibility)                                                                                     | Scroll-driven motion; use the `media` slot for meaningful imagery (`alt`) and consider reduced-motion users      | —                                                   |

### Layout and scrolling

| Component                                                        | Built-in behavior                                                                                               | Keyboard                                                                                       |
| ---------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------- |
| [QLayout](/layout/layout#accessibility)                          | Landmark skeleton via children: `<header>`, `<footer>`, `<aside>`, `<main>`                                     | —                                                                                              |
| [QHeader / QFooter](/layout/header-and-footer#accessibility)     | Real `<header>`/`<footer>` landmarks; hidden marginals leave the AT tree; reveal-hidden ones re-reveal on focus | Focus re-reveals                                                                               |
| [QDrawer](/layout/drawer#accessibility)                          | `<aside>` landmark; backdrop and opener strip hidden from AT; no focus trap in overlay mode                     | <kbd>Esc</kbd> closes modal-state drawers                                                      |
| [QPage](/layout/page#accessibility)                              | Renders the page's `<main>` — don't add another                                                                 | —                                                                                              |
| [QPageScroller](/layout/page-scroller#accessibility)             | Positioning wrapper — put a real button in the slot for keyboard access                                         | Via slotted button                                                                             |
| [QScrollArea](/vue-components/scroll-area#accessibility)         | Custom scrollbars hidden from AT; set `tabindex="0"` for keyboard scrolling                                     | Native scroll keys once focusable                                                              |
| [QSplitter](/vue-components/splitter#accessibility)              | Full WAI-ARIA window splitter: `role="separator"`, `aria-controls`, value ARIA                                  | Arrows resize (RTL-aware), <kbd>Home</kbd>/<kbd>End</kbd>, <kbd>Enter</kbd> collapses/restores |
| [QSlideItem](/vue-components/slide-item#accessibility)           | Swipe actions are pointer-only — provide a keyboard alternative                                                 | None built in                                                                                  |
| [QInfiniteScroll](/vue-components/infinite-scroll#accessibility) | Loads on native scroll; loading state not announced                                                             | Works with keyboard scrolling                                                                  |
| [QPullToRefresh](/vue-components/pull-to-refresh#accessibility)  | Pointer gesture; expose `trigger()` through a button for keyboard users                                         | Via your own button                                                                            |

Purely visual or renderless utilities with no accessibility surface: [QSpace](/vue-components/space#accessibility), [QPageSticky](/layout/page-sticky#accessibility), [QIntersection](/vue-components/intersection#accessibility), [QResizeObserver](/vue-components/resize-observer#accessibility), [QScrollObserver](/vue-components/scroll-observer#accessibility), [QNoSsr](/vue-components/no-ssr#accessibility), [QSlideTransition](/vue-components/slide-transition#accessibility) and [QResponsive](/vue-components/responsive#accessibility).

## External resources

- [WCAG 2.2](https://www.w3.org/WAI/WCAG22/quickref/) — the Web Content Accessibility Guidelines quick reference
- [WAI-ARIA Authoring Practices Guide (APG)](https://www.w3.org/WAI/ARIA/apg/) — the interaction patterns Quasar components follow
- [MDN Accessibility](https://developer.mozilla.org/en-US/docs/Web/Accessibility) — practical HTML/ARIA reference
- [WebAIM](https://webaim.org/) — articles, the contrast checker and screen reader survey data
- [axe DevTools](https://www.deque.com/axe/) and Lighthouse — automated auditing in the browser and CI
- [NVDA](https://www.nvaccess.org/) (Windows, free) and VoiceOver (built into macOS/iOS) — screen readers to test with
