/**
 * Pages reached by their URL alone: on no navigation, in no search
 * index and in neither markdown form (being on no navigation is what
 * keeps them out of the forms, build/md/nav-forms.js). Paths relative
 * to src/pages.
 */
export const UNLISTED_PAGES = ['guide.md']
