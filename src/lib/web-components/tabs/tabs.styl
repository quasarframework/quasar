
.tabs
  min-height 2rem
  overflow hidden

  color $tabs-color
  background-color $tabs-background-color
  font-size $tabs-font-size
  font-weight $tabs-font-weight
  transition color .18s ease-in, box-shadow .18s ease-in

  border-radius $generic-border-radius

  &:not(.scrollable)
    .left-scroll, .right-scroll
      display none !important

  .left-scroll, .right-scroll
    height 100%
    min-width 25px
    cursor pointer
    color $tabs-active-color
    font-size (1.2 * $tabs-icon-font-size)

    &.disabled
      visibility hidden

  if $theme == 'ios'
    @extend .justify-center

body.mobile .tabs.scrollable
  .left-scroll, .right-scroll
    display none !important
  .spacer
    min-width 45%

.tabs-scroller
  overflow hidden

.tab
  display flex
  min-height 55px
  padding $tabs-padding
  cursor pointer
  transition all .3s
  white-space nowrap

  .tabs-icon
    font-size $tabs-icon-font-size
  .tabs-icon + .tabs-label
    margin $tabs-label-margin
  &.active
    color $tabs-active-color
  &:active
    background-color $tabs-highlight-color

  if $theme == 'mat'
    flex-direction row
    text-transform $mat-tabs-text-transform
    border-bottom $mat-tabs-border-bottom

    &.active
      border-bottom $mat-tabs-active-border-bottom

  if $theme == 'ios'
    flex-direction column


if $theme == 'mat'
  @media only screen and (max-width $layout-small-max)
    .tab
      &.hideIcon .tabs-icon,
      &.hideLabel .tabs-label
        display none !important

@media only screen and (max-width $layout-medium-max)
  .tabs:not(.scrollable)
    .tabs-scroller, .tab
      flex 1 1 auto

@media only screen and (min-width $layout-big-min)
  .tab
    padding-left (3 * $tabs-padding[1])
    padding-right (3 * $tabs-padding[1])
