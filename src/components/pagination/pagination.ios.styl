.q-pagination
  display inline-flex
  input
    text-align center
    -moz-appearance textfield
  .q-if
    margin 8px 0 0 0
  .q-input-target
    width 100%
  .q-btn
    padding 0 5px !important
    &.disabled
      color $faded
