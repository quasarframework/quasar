{
  "meta": {
    "docsUrl": "https://v1.quasar.dev/vue-components/table"
  },

  "behavior": {
    "$listeners": true
  },

  "props": {
    "props": {
      "type": "Object",
      "desc": "QTable's header column scoped slot property",
      "examples": [ ":props=\"props\"" ],
      "category": "general"
    },

    "auto-width": {
      "type": "Boolean",
      "desc": "Tries to shrink header column width size; Useful for columns with a checkbox/radio/toggle",
      "category": "content"
    },

    "scope": {
      "type": "String",
      "desc": "Describes the scope of the cell; 'header' and 'footer' will be mapped to a col scope;",
      "default": "header",
      "values": [ "header", "footer", "row" ],
      "category": "behavior|style",
      "addedIn": "v1.13.0"
    }
  },

  "slots": {
    "default": {
      "extends": "default"
    }
  }
}
