{
  "meta": {
    "docsUrl": "https://v2.quasar.dev/vue-components/table"
  },

  "props": {
    "props": {
      "type": "Object",
      "desc": "QTable's header column scoped slot property",
      "examples": ["# :props=\"props\""],
      "category": "general"
    },

    "col-name": {
      "type": "String",
      "desc": "The 'name' of the column this header cell belongs to; Overrides relying on the Vue 'key' attribute, which cannot cross a wrapper component; Only meaningful when used in QTable's 'header' slot",
      "examples": ["# col-name=\"desc\""],
      "category": "general",
      "addedIn": "v2.27"
    },

    "auto-width": {
      "type": "Boolean",
      "desc": "Tries to shrink header column width size; Useful for columns with a checkbox/radio/toggle",
      "category": "content"
    }
  },

  "slots": {
    "default": {
      "extends": "default"
    }
  },

  "events": {
    "click": { "internal": true }
  }
}
