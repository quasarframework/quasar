import { createSizeStyle } from '../../composables/private.use-size/use-size.js'

// shared by QCheckbox / QToggle / QRadio
export const getOptionSizeStyle = /*#__PURE__*/ createSizeStyle({
  xs: 30,
  sm: 35,
  md: 40,
  lg: 50,
  xl: 60
})
